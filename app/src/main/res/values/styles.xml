<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="styleRecyclerView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:visibility">visible</item>

    </style>

    <style name="styleEditText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/editTextHeigh</item>
        <item name="android:textAlignment">center</item>
        <item name="android:paddingStart">@dimen/paddingStartEndEditText</item>
        <item name="android:paddingEnd">@dimen/paddingStartEndEditText</item>
        <item name="android:layout_marginStart">@dimen/layoutMarginStartEnd</item>
        <item name="android:layout_marginEnd">@dimen/layoutMarginStartEnd</item>
        <item name="android:background">@drawable/fondotextview</item>
        <item name="android:textColor">@color/text_primary_dark</item>
    </style>

    <style name="styleBtn">
        <item name="android:layout_height">@dimen/btnHeigh</item>
        <item name="android:layout_width">@dimen/btnWidth</item>
        <item name="android:layout_marginStart">@dimen/layoutMarginStartEnd</item>
        <item name="android:layout_marginEnd">@dimen/layoutMarginStartEnd</item>
        <item name="android:textColor">@color/text_primary_light</item>
        <item name="android:background">@drawable/btn</item>
    </style>

    <style name="styleBtnDialog">
        <item name="android:layout_height">@dimen/btnDialog</item>
        <item name="android:layout_width">@dimen/btnWidth</item>
        <item name="android:layout_marginStart">@dimen/layoutMarginStartEnd</item>
        <item name="android:layout_marginEnd">@dimen/layoutMarginStartEnd</item>
        <item name="android:textColor">@color/text_primary_light</item>
        <item name="android:background">@drawable/btn</item>
    </style>

    <style name="styleBtnDialogConfirm">
        <item name="android:layout_height">@dimen/btnDialog</item>
        <item name="android:layout_width">@dimen/btnWidthC</item>
        <item name="android:layout_marginStart">@dimen/layoutMarginStartEnd</item>
        <item name="android:layout_marginEnd">@dimen/layoutMarginStartEnd</item>
        <item name="android:textColor">@color/text_primary_light</item>
        <item name="android:background">@drawable/btn</item>
    </style>

    <style name="styleBtnLevel">
        <item name="android:layout_height">@dimen/btnHeigh</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_marginStart">@dimen/layoutMarginStartEnd</item>
        <item name="android:layout_marginEnd">@dimen/layoutMarginStartEnd</item>
        <item name="android:textColor">@color/text_primary_light</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:background">@drawable/btn</item>
    </style>

    <style name="styleText">
        <item name="android:textColor">@color/text_primary_dark</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
